{
  "name": "@ronriku/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "publish:content": "pnpm --filter @ronriku/core build && node scripts/publish.ts",
    "test": "pnpm --filter @ronriku/core build && vitest run",
    "test:db": "supabase test db",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ronriku/core": "workspace:*",
    "@supabase/supabase-js": "^2.117.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "bs58": "^6.0.0",
    "tweetnacl": "^1.0.3",
    "typescript": "~5.9.3",
    "vitest": "^3.2.7"
  }
}
