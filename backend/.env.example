# Copy to backend/.env.local (git-ignored). Values for the local stack come from `npx supabase status -o env`.
# If unset, scripts and tests read them from `npx supabase status -o json` automatically.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
